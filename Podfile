platform :osx, '10.7'

# ignore all warnings from all pods
inhibit_all_warnings!

project 'SwaggerToObjCGenerator'

target 'SwaggerToObjCGenerator' do
    pod 'Mantle'
    pod 'YAML-Framework'
end

target 'OpenAPItoObjC_CodeGen' do
    pod 'Mantle'
    pod 'YAML-Framework'
end
